# XAMPP MySQL (default: localhost:3306, user root, no password)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=inventory_management

# Optional: set to "true" to enable Sequelize SQL logging
DB_LOGGING=false

# Server
PORT=3000
NODE_ENV=development
